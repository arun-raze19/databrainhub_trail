# Production API URLs - Using mock data
# These URLs won't actually be used since we're using mock data
# But we need to set them to something to avoid undefined errors
VITE_API_URL=https://mock-api.example.com
VITE_SOCKET_URL=https://mock-api.example.com
